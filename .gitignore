# offline bake CI debris (the .bin artifacts + manifest ARE committed for GitHub Pages)
baked/shot-*.png
baked/status-*.txt

# OS noise
Thumbs.db
.DS_Store

# original Poly Haven downloads (huge; the 1k gltf conversions are committed)
assets/models/*.blend.zip